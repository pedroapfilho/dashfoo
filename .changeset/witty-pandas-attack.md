---
"@dashfoo/react": patch
---

Update the @xstate/react dependency to v6. No API surface changes — the hooks dashfoo uses (useActorRef, useSelector) are unchanged, and v6 stays peer-compatible with xstate ^5.28 and React 18/19.
