---
"@dashfoo/react": patch
---

Enlarge floating-panel resize hit areas. Edge bands are now 12px (was 6px) and
straddle the frame border evenly, and corners are 20px (was 14px), so the
side and bottom edges are far easier to grab with the mouse. A dimmed resize-grip
cue is shown in both bottom corners to signal where to drag.
