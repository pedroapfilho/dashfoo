---
"@dashfoo/react": minor
---

Move required layout, floating-panel geometry, resize handles, and docking indicators into the exported `@dashfoo/react/styles.css` stylesheet. Import it once before an optional theme. Runtime positions use CSS custom properties, including pointer gestures and cancellation.
