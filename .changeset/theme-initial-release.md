---
"@dashfoo/theme": minor
---

Initial release of the default skin: framework-agnostic plain CSS over overridable `--dashfoo-*` tokens (`dashfoo.css` and `tokens.css` entry points), light by default with an opt-in dark variant via `data-dashfoo-theme="dark"`.
