---
"@dashfoo/theme": patch
---

Round the top corners of tab items so tabs read as attached to the tabset surface, and introduce a `--dashfoo-radius-sm` token (0.375rem) for nested chrome — tab corners, the tab focus ring, and the overflow/drag chips now resolve through it instead of a hardcoded radius.
