# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets). Run `pnpm changeset` to record a change and its semver bump; the release workflow turns accumulated changesets into version bumps and npm publishes.
