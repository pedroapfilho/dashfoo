---
"@dashfoo/react": patch
---

Bump the bundled `@dnd-kit/dom` from `0.4.0` to `^0.5.0`. No API or drag-behavior changes — the drag adapter and the full Playwright drag/dock e2e suite pass unchanged.
