// Public API barrel for @dashfoo/theme.
// Populated in Phase 7 (Base-UI-composed skin over the @dashfoo/react headless
// components). Placeholder export keeps the tsdown build entry valid until then.
// The design tokens ship separately as ./tokens.css.

export const DASHFOO_THEME_VERSION = "0.0.0" as const;
