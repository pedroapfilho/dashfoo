// Public API barrel for @dashfoo/core.
// Populated in Phase 2 (schema → reducer → history → serialize → geometry)
// and Phase 3 (the XState actor system). Placeholder export keeps the tsdown
// build entry valid until then.

export const DASHFOO_CORE_VERSION = "0.0.0" as const;
